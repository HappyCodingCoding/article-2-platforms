# 公众号标题工具总一个味儿？我把起标题拆成 11 步

你有没有过这种时刻——

打开任何一个 AI 起标题工具，把你刚刚熬了一周写出来的文章粘进去，按下"生成"。10 秒之后，屏幕上整整齐齐排着 10 个标题。

你把它们扫一遍，每个都像那么回事。又扫一遍，发现它们全都一个味儿。

摆在一起看，是 10 个标题；  
分开来看，是同一个标题换了 10 套衣服。

---

这种感觉学名叫 **"AI 起标题体"**：

> 结尾挂着"必看""干货"或"不容错过"；  
> 情绪用了"赋能""价值""核心"；  
> 人群用了"职场人""年轻人""宝妈"；  
> 没有观点、没有反常识、没有一个你愿意截图发朋友圈的句子。

最让你难受的不是它写得差——是你知道它本来就**只能**写成这样。它压根儿就不知道"好的公众号标题"长什么样，它只见过"网上那些标题长什么样"。

我去年被这件事折磨了大半年，然后做了一份 Skill。

它不是一个 prompt，也不是一个公式表。它把"起一个公众号标题"拆成了一条 **11 步流水线**：19 套句式 × 10 种心理触发器，所有候选标题进来先过三道闸，再按 5 种角色分类输出。

今天把它拆给你看。

---

## 一、这份 Skill 长什么样？它和市面上的同类有什么不一样

我先把市面上能见到的五类起标题工具放桌上，给你做个对比——这份 Skill 跟它们各自的根本分歧在哪里。

| 类型                  | 它在做的事                  | 这份 Skill 在做的事                        |
| ------------------- | ---------------------- | ------------------------------------ |
| **"震惊体一键生成"**       | 套 3 个模板，关键词填空          | 19 个公式各自有槽位、范例、AI 废稿对照，每条都从语义层重写     |
| **"GPT prompt 调参"** | 把"你是一个公众号爆款标题写手"塞进系统提示 | 起标题不是一个 prompt 任务，是 11 步工程化流程        |
| **"标题库检索"**         | 把别人的 10w+ 标题搜出来给你抄     | 不抄别人，给你的内容做**专属**的 brief，再从 brief 出题 |
| **"AI 改写 / 评分"**    | 给你现有标题打个分就完事           | 不止打分，能在保持"保留资产"的前提下做诊断—重写            |
| **"方法论文章"**         | 告诉你"数字标题效果好"，但不教你怎么用   | 11 步流程里强制每一条都做"为什么这个公式适合这篇内容"        |

最显眼的三个差异——

**第一个差异：它是双轴组合，不是一个公式。**

市面上大多数工具给你一个公式名（"反常识""数字化""对比"），然后让你自己回去头脑风暴。

这份 Skill 里，公式和心理机制是**两个独立池子**——19 个公式 × 10 个心**理触发器**，组合出 **190 种**候选起手式。再用一个多样性上限锁死重复，确保你拿到的 10 个标题不会全是同一个配方换皮。

**第二个差异：它是流水线，不是一次生成。**

"给我一篇内容，输出 10 个标题"这种 prompt 是无状态的——它不会记住"刚才这条配的什么句式"，所以输出天然会撞车。

这份 Skill 把过程拆开：  
先读你的内容 → 建一份 brief（核心对象、人群行为标签、反常识点、证据资产、事实边界）→ 分别给公式和机制按内容适配度打分 → 网格化配对 → 排序选 10 条 → 每条进三道闸 → 按 5 种角色分类。

每一步都有显式的产出，下一步拿上一步的产出继续，不会后面忘了前面。

**第三个差异：它有"质检部门"，没有内部化作弊。**

工程层面的硬约束（20–26 字、合规红线）我写成了脚本，跑就完了。  
语义层面的检查（三秒看懂、不是 AI 味儿、人群行为锚定而不是人群标签）写成了 6 条规则，配 ❌/✅ 实例对照，每条候选都要对一遍。

最狠的一招——**19 个公式每个都配了一个公开承认的 AI 废稿**。比如"痛点直击型"的废稿是《30岁职场人必看的时间管理技巧》（15 字），废就废在"三槽全空"。

这些废稿永远不进你的输出。它们存在的唯一目的，是让你（和 AI 自己）在写的当下，就能识别出"我这一稿是不是又写成废稿的样子了"。

---

## 二、它是怎么保证标题质量的

这一节只讲一件事：**为什么它出来的标题，比市面上那种"10 个看起来不一样的'必看'"更值得发**。

### 1. 三道闸——硬的不行，软的不行，三秒看不进去的也不行

每一条候选标题都要过这三道闸：

**闸一·字符闸（脚本强制）**

公众号订阅号列表里，标题展示宽度是固定的。19 个字以下空荡荡，27 个字以上被截断。

这条 Skill 把展示宽度直接量成了硬区间——**20 到 26 个字符**，含标点。脚本里直接 `FAIL`，过不去的不进下一步。

**闸二·合规闸（脚本硬层 + 人工软层）**

"最""第一""唯一"这些极限词归硬层——脚本抓到就 FAIL，不商量。

擦边蹭、主观感受写成客观效果、未证实的负面指控——归软层 WARN，留给人判断。脚本不替你做这个决定。

**闸三·三秒闸（语义判断，三问）**

这是最后一道，也是最难代写的一道。三问：

- **第一秒**——读者扫过去，能立刻判断"这写给我的"吗？
- **第二秒**——他能感觉到"这能解决我现在的痛 / 带给我想要的回报"吗？
- **第三秒**——他能感觉到"不点会错过什么"吗？

任何一问答不上来，这稿就回炉。

三问合在一句话里：**它得让订阅号列表里那一秒扫过去的拇指停下来。**

### 2. 6 条去 AI 味规则——每一条都有 ❌/✅ 实例

这些规则不靠脚本，写在 `references/de-ai-writing.md` 里，起草前先读完，下面给三条最常翻车的：

**Rule 1 — 说人话，别用名词去命名一种情绪**

> ❌ 《中年人的压力与责任：如何平衡工作和生活》（19 字）
>
> ✅ 《成年人的崩溃都是静音模式，收场也得自己一个人来》（23 字）

"压力""责任"是 AI 在**命名**一种情绪；"崩溃""静音模式""自己一个人来"是人在**描述**那种情绪发生的现场。

**Rule 2 — AI 高频词清单，先问一句"我是不是在偷懒"**

> 深入、关键、核心、必备、必看、搞定、轻松、快速、高效、提升、打造、解锁、分享、推荐、建议、究竟、到底、背后、干货、赋能、助力、一文读懂、全面解析、值得拥有、的重要性。

这 26 个不是禁用。它们是"偷懒信号"——AI 默认会伸手拿它们。**每次落笔前，问自己一句：这一句**真的需要**这个字吗？还是我懒得想了？**

**Rule 3 — 行为标定人群，别贴人群标签**

> ❌ 《30 岁职场人必看的时间管理技巧》（15 字）
>
> ✅ 《天天加班到 10 点的打工人，别再用下班时间搞时间管理了》（26 字）

第一句的反应："这好像跟我有点关系。"  
第二句的反应："**这说的不就是我吗？**"

读两次，差别就出来了。

完整 6 条规则太占篇幅，这里只放三条。剩下三条分别在「数字不能独立成钩」、「观点要敢冒犯」、「悬念必须罩得住读者想要的答案」。这条 Skill 在起草前会把这 6 条全读一遍——不是事后检查，是下笔前的语感预设。

### 3. AI 废稿对照组——每个公式都自带失败样本

这是我觉得最值得拿出来吹的一项工程化设计。

19 个公式，**每一个**下面都挂了一条 AI 废稿。这些废稿：

- 都是真的——不是临时编的"反例"。
- 都通过了字符闸。
- 都默认会被任何一家 AI 工具作为"标准答案"输出。

举两个：

> **痛点直击型**的废稿：《30岁职场人必看的时间管理技巧》（15 字）  
> ——失败原因：泛人群 / 无痛点 / 无预期 / 三槽全空。**长度不达标从来不是长度问题，是槽位问题。**

> **热点绑定型**的废稿：《Deepseek 火爆全网：AI 浪潮下的思考》（18 字）  
> ——失败原因：前半句是公共废话、后半句是空泛抒发，两个半句都没有读者能截图走的金句。

这些废稿不进你的输出。它们只在你（和 AI 自己）写的当下当**镜子**用——看一眼自己这一稿，是不是又"长得像那条废稿了"。

### 4. 事实边界——brief 里缺的东西，永远不会被编出来

Brief 里有一项叫"**事实边界**"——结论、数字、效果、排名、绝对化表述里，**正文不能证实**的部分。

这条 Skill 在底层写死：事实边界里列出来的内容，永远不许进标题。

它不能凭空给你加一个"实测提升 300%"——因为正文没测过。  
它不能给你挂一个"刚刚上市"——因为正文没说今天。  
它不能给你"全网首发"——除非 brief 里有。

把"瞎编的爆款"和"撑得住的爆款"分开，是这份 Skill 做的最克制、也最重要的一件事。

---

## 三、它的工作原理——为什么是"流水线"，不是"一次生成"

一句话讲清：**起一个公众号标题，不是文案任务，是工程任务。**

文案任务可以一次 prompt 解决。工程任务不行，因为它的输入空间和约束空间都太复杂。

**输入**：一篇内容（几千字到几万字都有），一段目标行为（点击 / 转发 / 评论 / 收藏 / 自动推断），一些隐性偏好（要副标题？要多还是少？）。

**约束**：

- 字符 20–26
- 极限词不能用
- 三秒要让读者停下来
- 不能是 AI 味儿
- 不能给凭空数据
- 不能太重复——10 个候选里不能有两个看上去是同一个标题换皮
- 还得适配你想要的读者行为

一个 prompt 同时满足 7 个约束，概率是 0。

所以拆成流水线：

```
┌──────────────────┐
│ ① 读内容 → 建 brief  │
└────────┬─────────┘
         ▼
┌──────────────────┐
│ ② 按内容给 10 个心理机制打分，排序入池 │
└────────┬─────────┘
         ▼
┌──────────────────┐
│ ③ 按内容给 19 个公式打分，排序入池    │
└────────┬─────────┘
         ▼
┌──────────────────────────────┐
│ ④ 网格配对：公式 × 机制 → 选 top tactics │
│    多样性上限：单个公式 / 单个机制   │
│    不超过候选数的 30%                    │
└────────┬───────────────────────┘
         ▼
┌──────────────────────────────┐
│ ⑤ 顺序起草，每条过：                  │
│    字符闸（脚本）                    │
│    合规闸（脚本硬层 + 人工软层） │
│    三秒闸（语义判断，三问）         │
│    6 条去 AI 味规则                  │
└────────┬───────────────────────┘
         ▼
┌──────────────────────────┐
│ ⑥ 评分 + 去重 + 风险定级        │
└────────┬───────────────────────┘
         ▼
┌──────────────────────────┐
│ ⑦ 按 5 种角色分类输出          │
└────────┬───────────────────────┘
         ▼
┌──────────────────────────┐
│ ⑧ Top 5 一览 + A/B 测试建议    │
└──────────────────────────┘
```

**为什么这套设计比一次 prompt 强？**

- **不重复**——多样性上限是硬编码的，10 条候选里同一个公式最多出现 3 条。一个 prompt 完全做不到这件事。
- **不撞脸**——心理机制池独立打分，**强冲突型**和**治愈型**不会同时进你的候选池。
- **不自欺**——三道闸里有两道是脚本硬跑，第三道是结构化语义判断。没有任何一个环节是"凭感觉"。
- **不出错**——事实边界从 brief 第一步就锁死，剩下 10 步都不允许越界。

最重要的——**可审计**。每一步都留下了"我这一步实际做了啥"的产物，最后汇总成一份"流程对照表"。你哪一步想质疑，直接打开产物看。

---

## 四、它的工作流程——11 步，每一步都有显式产出

把上一节那张图展开，就是下面这张表：

| 步骤                    | 干什么                                                                | 显式产出                  |
| --------------------- | ------------------------------------------------------------------ | --------------------- |
| 1                     | 解析你的输入（文件 / 主题 / 一段文字 / 现有标题），确定模式（draft/rewrite）和其它选项             | 输入形态 + 模式 + 推断说明      |
| 2                     | 把内容提进一份 brief：核心对象、人群行为标签、反常识点、回报、证据、冲突、情绪、时效、事实边界、合规风险            | 8–10 行 brief，缺的地方标"缺" |
| 3（仅 rewrite）          | 诊断你的现有标题——字数、人群锚、痛点、张力、口语化、合规风险，列出"保留资产"                           | 一份诊断清单                |
| 4                     | 给 10 个心理机制按内容适配度打分（1–10，6 分及以上入池），按分数排序                            | 一份"机制池"               |
| 5                     | 给 19 个公式按内容适配度打分，6 分及以上入池，按分数排序                                    | 一份"公式池"               |
| 6                     | 机制池 × 公式池 = 全网格，合并打分；多样性上限约束（单公式 ≤ 30%、单机制 ≤ 30%），选 top N          | 一份"战术清单"（一份公式 × 机制）   |
| 7                     | **起草前**通读 6 条去 AI 味规则，把规则内化为语感                                     | （语感预设）                |
| 8                     | 按战术清单顺序起草——每条做：① 起草；② 加料 / 叠公式 / 加辣；③ 全条过 6 条规则；④ 三道闸；失败 3 次封顶保留最佳 | `--count` 条候选         |
| 9                     | 给每条打分（不凭感觉，按维度）；去重；风险定级（低/中/高）；按 5 种角色强制分类，每类至少有一条                 | 5 个分类组，组内按分排序         |
| 10（仅 `--subtitle on`） | 把每组冠军配成 `主标题｜副标题`，整段仍按 20–26 字预算走                                  | 主＋副标题组合 2–3 条         |
| 11                    | 渲染输出（brief、矩阵、Top 5、A/B、流程对照）                                      | Markdown 输出           |

**几个流程上的硬约束**：

- **第 7 步不能跳过**。6 条规则读到烂熟之前不能下笔。这条 Skill 在系统提示里写死了——"不是事后检查，是下笔前的语感预设"。
- **第 8 步每条最多改 3 次**。第 3 次还过不去，就当"最佳品"留下，但**必须**明明白白标注哪道闸还没过。这样下一步的评分会读到它，不会偷偷放过去。
- **第 9 步的高分坑**：综合型（最稳健那个角色）的冠军，必须是低/中风险——这条晋升门禁是为了不让你拿到一个"一眼最炸"的标题就成了综合冠军，它是炸，但不稳。

流程的最后一步不是交付，是**自审计**。每一步发生了什么、跳过了什么、触发了什么条件（候选不够、闸门刷人、最佳品保留……），全部写在"流程对照"那张表里。

这一段单独再点一次：你交付一篇内容给 Skill，Skill 不会只扔 10 个标题给你。它扔一个**完整审计记录**给你。任何一步不放心，你打开就能看。

---

## 五、怎么用——三个最常见场景

这份 Skill 的设计目标不是"按一个按钮"，是"按你的内容形态挑最合适的入口"。最常见的三个使用场景如下——

### 场景 1：你刚写了一篇文章，要 10 个候选标题

把文章保存成本地文件，比如 `~/articles/2026-09-04-xxx.md`，然后告诉 Skill：

> "给 ~/articles/2026-09-04-xxx.md 这篇文章起 10 个公众号标题。"

Skill 会：

1. 读完整篇文章 → 建 brief
2. 自动推断 `--mode draft --action auto`
3. 把 11 步走完

你收到的是一份含 5 个分类组的候选矩阵，每组冠军在 Top 5 里给你点出原因，最后还附 2–3 组 A/B 测试建议。

### 场景 2：你已经写好了一个标题，想优化

把你现在的标题贴过来，加一句"帮我看看这个"或者"改一下"。

Skill 会：

1. 自动切到 `--mode rewrite`
2. **诊断**——字数、人群锚、痛点、张力、口语化、合规
3. **识别保留资产**——你标题里那些对的字眼（一个动词、一个真数字、一个精准的人群词），改写时不能动
4. 给你一个改写版 + 改写理由（"把『中年人』换成『38 岁才决定转行的中年产品经理』，人群从标签变成行为锚"）

这一步的好处是它**不毁你**——你原来的标题里那些好东西，它会原样保留。

### 场景 3：特定目标行为 + 主＋副标题

内容是干货方法论，需要副标题展开具体价值；或者目标行为偏转发，不是点击。

```
"给 ~/articles/xxx.md 起 10 个标题，要 5 个偏转发的（--action share）；
干货文，开主＋副标题（--subtitle on）。"
```

它会按 5 类分完之后，把每类冠军配上副标题——主标题挂情绪 / 反转，副标题给具体的回报承诺。中间用 `｜` 分隔，**整段仍按 20–26 字预算走**，不会超。

### 三个有用的开关

| 选项                                | 什么时候用                              |
| --------------------------------- | ---------------------------------- |
| `--count 6`–`20`                  | 想要少而精就 6，想要广撒网就 15–20（再高就开始撞公式了）   |
| `--subtitle on`                   | 干货 / 方法论文，且你愿意在公众号里用主副标题           |
| `--action share / comment / save` | 内容本身不是为了点击设计的，转发 / 评论 / 收藏 是更现实的目标 |

完整的选项说明都写在 `SKILL.md` 里。最重要的一条：**选项值如果你没明说，Skill 会自己推断，并且在跑之前告诉你它推断出了什么**。你能立刻看到、立刻否决、立刻改。

---

## 写在最后——你不需要再多一个 AI 起标题工具

去年开始做这份 Skill 的时候，我跟人介绍它的方式是："我用一年的时间踩遍标题坑，把经验压成了一条流水线。"

这句话被同事翻来覆去笑了三个月——"你这不就是一个 prompt 嘛。"

我后来放弃了争论。没在市面上看到一个工具**同时**满足下面 4 件事：

1. **有公式也有触发器**——双轴设计，单凭"反常识"或单凭"数字化"出不来好标题。
2. **有质检也有多样性约束**——三道闸门 + 多样性上限，一起解决"全是同一个味儿"和"全是同一个句式"。
3. **有失败样本也有保留资产**——AI 废稿教你识别失败模式，rewrite 模式保留你原来标题里的金子。
4. **有产出也有审计记录**——你看完 10 个候选不满意，能直接打开流程对照表，质疑具体某一步。

这份 Skill 不是一个新工具。它是**一年来我能从"安慰自己加班做内容"走到"被订阅号列表里的人看见"**&#x7684;那段流程，被结构化、工程化、可复现之后的样子。

你不需要再多一个 AI 起标题工具——  
你可能需要的是把"起标题"这件事，从文案任务重新认成工程任务，  
然后照着工程任务的样子搭一条小流水线。

这份 Skill 是这么搭的。  
现在它对你开源，欢迎拿走。

---

**写在最后的最后**：挑标题的时候，我把自己起的那一版拿去 Skill 里跑了一遍——

> 小计：10 条，PASS 9 / FAIL 1。

> FAIL 那一版写的是"我做了个公众号标题 Skill，11 步流水线，每条都过三道闸"，31 字。脚本直接判它超长："删的应该是修饰语，不是承载信息的槽位。"

我按它的话把修饰语砍了，留下来这一条——  
**《公众号标题工具总一个味儿？我把起标题拆成 11 步》**，25 字。

所以你看，Skill 不会骗你说"差不多就行"。它连自己的宣传稿都不放过。
